/// Barrel export for `test/fixtures/`.
///
/// Empty for now — every file in this directory is a stub (see each
/// file's doc comment) because the M0 Foundation epic (#4) excludes
/// domain models, and there is nothing to export until M1 lands them.
///
/// TODO(M1): once the fixture files above have real bodies, export them
/// here:
/// ```dart
/// export 'daily_log_fixture.dart';
/// export 'meal_entry_fixture.dart';
/// export 'streak_state_fixture.dart';
/// export 'symptom_log_fixture.dart';
/// ```
library;
