<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- M15's macro estimation and M16's menu analysis are the app's only
         outbound calls, both over LlmChatClient. Flutter's template declares
         INTERNET in the debug and profile overlays only, "required for
         development" - so debug builds could reach the network and a release
         build never could. Without it the socket is refused before a packet
         leaves the device, and the refusal reaches the user as
         "no internet" on a working connection: character for character the
         symptom of #419, which took three issues to diagnose for exactly that
         reason. A Keto Lens scan still makes no network call - Epic #10's
         invariant is about what the scanner does, not about what the app is
         permitted to do. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Keto Lens. CAMERA is requested at run time by the `camera` plugin's
         own initialize() call, which is why permission_handler is not a
         dependency - design/m6_preflight.md 2.2. READ_MEDIA_IMAGES is the
         API 33+ replacement for READ_EXTERNAL_STORAGE and is what
         image_picker needs to import a label photo already taken. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- The scanner is the point of the app, but it is one tab of five: the
         rest works without a camera, so do not let the Play Store hide the
         app from a device that has none. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- The 20:00 streak reminder. Since flutter_local_notifications 16 the
         plugin declares only POST_NOTIFICATIONS and VIBRATE in its own
         manifest; everything a *scheduled* notification needs is the app's to
         declare, and none of it was. Without the two receivers below the
         alarm fires into nothing and no notification is ever shown, which is
         indistinguishable from the user having denied permission.

         RECEIVE_BOOT_COMPLETED plus ScheduledNotificationBootReceiver is what
         re-arms the pending reminder after a reboot. Without it the reminder
         stops at the first restart and the app has no way to notice - it only
         reschedules at launch, and a phone that reboots overnight is not
         launched before 20:00.

         No SCHEDULE_EXACT_ALARM or USE_EXACT_ALARM: the reminder is scheduled
         inexactly (see StreakNotificationService), so it needs neither. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="Fantastic"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Shows the scheduled reminder when its alarm fires. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <!-- Re-arms pending reminders after a reboot or an app update. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
