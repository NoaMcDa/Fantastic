distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
# Gradle 8.14.3, not the template's 9.3.1. Gradle 9 removed `jcenter()`, and
# `flutter_tesseract_ocr` 0.4.31 still calls it — see the long note in
# settings.gradle.kts for the whole argument. 8.14.3 is the newest 8.x, is
# above Flutter 3.47.3's hard error floor of 8.14.0, and is new enough for
# AGP 8.13.1 (which requires 8.13+).
distributionUrl=https\://services.gradle.org/distributions/gradle-8.14.3-all.zip
