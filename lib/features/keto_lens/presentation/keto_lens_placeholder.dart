import 'package:flutter/material.dart';

class KetoLensPlaceholder extends StatelessWidget {
  const KetoLensPlaceholder({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('מצלמה')));
}
