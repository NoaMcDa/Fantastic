import 'package:flutter/material.dart';

class AdaptationPlaceholder extends StatelessWidget {
  const AdaptationPlaceholder({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('התאמה')));
}
