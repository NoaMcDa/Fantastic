import 'package:flutter/material.dart';

class RecipePlaceholder extends StatelessWidget {
  const RecipePlaceholder({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('מתכונים')));
}
