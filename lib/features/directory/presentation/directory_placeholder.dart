import 'package:flutter/material.dart';

class DirectoryPlaceholder extends StatelessWidget {
  const DirectoryPlaceholder({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('ספרייה')));
}
